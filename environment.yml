name: hetshuffle
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy<2
  - scipy
  - astropy
  - astroquery
  - matplotlib
  - reproject
  - psycopg2
  - pip
  - pip:
      # pyds9 and ds9 may require X11; install via conda-forge if needed:
      # - pyds9
      
# Notes:
# - For optional Oracle support, install cx_Oracle:
#   conda install -c conda-forge cx_oracle
